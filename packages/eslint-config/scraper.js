import { config as baseConfig } from "./base.js";

/**
 * ESLint configuration for the scraper app.
 *
 * @type {import("eslint").Linter.Config[]}
 */
export const config = [...baseConfig];

export default config;
